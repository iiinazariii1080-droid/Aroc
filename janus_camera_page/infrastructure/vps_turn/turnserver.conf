# ─── coturn / TURN server config ────────────────────────────────────
# Hardened 2026-03-04 (P1.6 + P1.7)

# ── Listeners ───────────────────────────────────────────────────────
listening-port=3478
tls-listening-port=443
# 5349 kept as alt for backward compat (may not bind with single IP)
alt-tls-listening-port=5349

listening-ip=82.165.177.194
relay-ip=82.165.177.194
external-ip=82.165.177.194

# ── Authentication ──────────────────────────────────────────────────
realm=techvisioncloud.pl
fingerprint

# Ephemeral HMAC-SHA1 credentials (coturn REST API / use-auth-secret)
use-auth-secret
static-auth-secret=e3ec4ead6a41e9b631c8dbd4c3b4ac0e

# Nonce lifetime — force re-auth every 600s
stale-nonce=600

# ── TLS ─────────────────────────────────────────────────────────────
cert=/etc/coturn/certs/fullchain.pem
pkey=/etc/coturn/certs/privkey.pem

# Disable old/insecure TLS versions
no-tlsv1
no-tlsv1_1

# ── Relay port range ───────────────────────────────────────────────
min-port=49152
max-port=65535

# ── Security: deny relay to private/multicast networks ─────────────
no-multicast-peers

denied-peer-ip=0.0.0.0-0.255.255.255
denied-peer-ip=10.0.0.0-10.255.255.255
denied-peer-ip=100.64.0.0-100.127.255.255
denied-peer-ip=127.0.0.0-127.255.255.255
denied-peer-ip=169.254.0.0-169.254.255.255
denied-peer-ip=172.16.0.0-172.31.255.255
denied-peer-ip=192.0.0.0-192.0.0.255
denied-peer-ip=192.88.99.0-192.88.99.255
denied-peer-ip=192.168.0.0-192.168.255.255
denied-peer-ip=198.18.0.0-198.19.255.255
denied-peer-ip=198.51.100.0-198.51.100.255
denied-peer-ip=203.0.113.0-203.0.113.255
denied-peer-ip=240.0.0.0-255.255.255.255

# ── Rate limiting ──────────────────────────────────────────────────
user-quota=50
max-bps=0
bps-capacity=0

# ── Logging ────────────────────────────────────────────────────────
syslog
log-file=/var/log/coturn/turnserver.log
simple-log

# ── Misc ───────────────────────────────────────────────────────────
# Disallow peers on the server's own IP (loopback relay attack)
no-loopback-peers
