global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.prometheus.yml

scrape_configs:
  - job_name: nav2adapter-reliability
    metrics_path: /ops/reliability.prom
    static_configs:
      - targets:
          - host.docker.internal:7905
