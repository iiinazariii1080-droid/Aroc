[pytest]
asyncio_mode = auto
testpaths = tests
addopts =
    --strict-markers
    --tb=short
    -q
markers =
    unit: fast isolated unit tests
    integration: tests that exercise multiple modules or HTTP
    hardware: requires real hardware connections
    slow: tests that take > 5 s
