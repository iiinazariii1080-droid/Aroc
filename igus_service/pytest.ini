[pytest]
asyncio_mode = auto
testpaths = tests
addopts = --cov=app --cov-report=term-missing:skip-covered --cov-fail-under=60
markers =
    simulator: requires simulator (main-2.py) and app (main.py) running as subprocesses
